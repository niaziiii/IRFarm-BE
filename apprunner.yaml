version: 1.0
runtime: docker
build:
  commands:
    pre-build:
      - echo "Installing build dependencies"
    build:
      - echo "Building Docker image"
run:
  runtime-version: latest
  command: npm start
  network:
    port: 8000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
