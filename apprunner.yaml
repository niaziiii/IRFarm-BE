version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - echo "Installing dependencies"
      - npm install
      - echo "Build completed"
run:
  runtime-version: 20
  command: npm start
  network:
    port: 8000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
